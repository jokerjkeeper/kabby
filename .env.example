# kabby 設定範例 — 複製成 .env 後填值（.env 已被 .gitignore，不會入 repo）
# daemon 啟動時自動載入（Node 20.12+ 內建 loadEnvFile）

# 認證 token（= 登入密碼）。設了之後 /api 與 WebSocket 都需要它。
# 本機也建議設。產生強隨機值：  node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
AUTH_TOKEN=

# 監聽位址。預設 127.0.0.1（只聽本機，最安全；GCP 上由 cloudflared 連 localhost）。
# 想從區網其他裝置直接連才改 0.0.0.0（注意：等於對整個區網開放，務必搭配 AUTH_TOKEN）。
# HOST=127.0.0.1

# 監聽埠
# PORT=3700

# CORS 跨域白名單（逗號分隔）。給「在別的 origin 跨域打 kabby /api」的前端用（例：本機 wepages）。
# 留空 = 放行全部（本機開發方便）。部署時務必設成實際 origin。
# KABBY_ALLOWED_ORIGINS=http://localhost:5002,http://127.0.0.1:5002

# cc 對話歷史 viewer 的可執行檔路徑（可留空）
# KABBY_VIEWER_PATH=
